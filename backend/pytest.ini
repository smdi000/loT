[pytest]
# On this Windows workspace pytest completes all test calls but its optional
# cacheprovider blocks while creating the cache's temporary directory during
# session finish.  Disable only that cache plugin; test discovery, fixtures,
# assertions, and teardown still run normally.
addopts = -p no:cacheprovider --basetemp=pytest_runtime
tmp_path_retention_policy = none
